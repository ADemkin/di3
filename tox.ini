[tox]
envlist = py310, py311, py312, py313, py314
skip_missing_interpreters = true

[testenv]
installer = uv
allowlist_externals = uv
commands_pre =
    uv pip install .
commands =
    pytest
deps =
    pytest
